### Баг репорт

1. #### BUG-1
    
    Название: Отображение счетчика со значением -1

    Приоритет: High

    Окружение: Linux/ Google Chrome Version 119.0.6045.159 (Official Build) (64-bit)

    Описание:
При установке значения -1 для счетчика на странице счетчиков на десктопной версии, счетчик отображает значение -1 вместо ожидаемого значения 0. Это приводит к некорректному отображению данных.

    Шаги воспроизведения:
    1. Зайти на страницу счетчиков на десктопной версии.
    2. Сделать подмену значений счетчиков на значения: -1.
    3. Сделать скриншот каждого счетчика после установки значения -1.

    Ожидаемый результат:
Счетчик должен отображать либо нулевое значение, либо NaN, при установке значения -1. Отображение значения -1 является ошибочным и не соответствует ожидаемому поведению.

    Фактический результат:
Счетчик отображает значение -1 вместо ожидаемого 0 или NaN.

    Скриншоты:
    ![](output/screen_test11_2024.04.14_16.59.03.png)

    Дополнительная информация:
Проблема наблюдается на всех счетчиках при установке значения -1. Требуется исправить отображение счетчиков для корректного отображения данных.

    Предложенное решение:
Исправить отображение счетчиков таким образом, чтобы при установке значения -1 они отображали либо нулевое значение, либо NaN, а не уходили в отрицательные значения.


2. #### BUG-2

    Название: При превышении максимального значения на счетчике больше не обновляются значения

    Приоритет: High

    Окружение: Linux/ Google Chrome Version 119.0.6045.159 (Official Build) (64-bit)

    Описание:
При превышении максимального значения для счетчика на странице счетчиков на десктопной версии, счетчик отображает значение 10 вместо ожидаемого значения 0. Это приводит к некорректному отображению данных.

    Шаги воспроизведения:
   1. Зайти на страницу счетчиков на десктопной версии.
    2. Сделать подмену значений счетчиков на значение, превышающее допустимое.
    3. Сделать скриншот каждого счетчика после установки значения.

    Ожидаемый результат:
Счетчик должен отображать либо нулевое значение, либо NaN. Отображение значения 10 является ошибочным и не соответствует ожидаемому поведению.

    Фактический результат:
Счетчик отображает значение 10 вместо ожидаемого 0 или NaN.

    Скриншоты:
    ![](output/screen_test16_2024.04.14_18.15.26.png)

    Дополнительная информация:
Проблема наблюдается на всех счетчиках при установке значения. Требуется исправить отображение счетчиков для корректного отображения данных.

    Предложенное решение:
Исправить отображение счетчиков таким образом, чтобы при установке значения они отображали либо нулевое значение, либо NaN.

3. #### BUG-3

    Название: Значение на счетчике не переводятся в более крупную систему измерения при значении 999999 и неверное округление счетчиков при значении 999999

    Приоритет: Medium

    Окружение: Linux/ Google Chrome Version 119.0.6045.159 (Official Build) (64-bit)

    Описание:
При значении 999999 для счетчика на странице счетчиков на десктопной версии, счетчик отображает значение 1000. Это приводит к некорректному отображению данных.

    Шаги воспроизведения:
   1. Зайти на страницу счетчиков на десктопной версии.
    2. Сделать подмену значений счетчиков на значение 999999.
    3. Сделать скриншот каждого счетчика после установки значения.

    Ожидаемый результат:
Счетчики становятся черным
При переходе пограничных значений счетчики переводятся в более крупную систему измерения

    Фактический результат:
Счетчик отображает значение 1000.

    Скриншоты:
    ![](output/screen_test4_2024.04.14_16.56.23.png)

    Дополнительная информация:
Проблема наблюдается на всех счетчиках при установке значения. Требуется исправить отображение счетчиков для корректного отображения данных.

    Предложенное решение:
Исправить отображение счетчиков таким образом, чтобы при установке значения они отображали корректное значение.

4. #### BUG-4

    Название: Значение 1000000 на счетчике отображается некорректно

    Приоритет: High

    Окружение: Linux/ Google Chrome Version 119.0.6045.159 (Official Build) (64-bit)

    Описание:
При значении 1000000 для счетчика на странице счетчиков на десктопной версии текст на счетчике энергии перекрывает друг друга. Это приводит к некорректному отображению данных.

    Шаги воспроизведения:
   1. Зайти на страницу счетчиков на десктопной версии.
    2. Сделать подмену значений счетчиков на значение 1000000.
    3. Сделать скриншот каждого счетчика после установки значения.

    Ожидаемый результат:
Счетчики становятся черным
При переходе пограничных значений счетчики переводятся в более крупную систему измерения

    Фактический результат:
Текст на счетчике энергии перекрывает друг друга.

    Скриншоты:
    ![](output/screen_test5_2024.04.14_16.57.16.png)

    Дополнительная информация:
Проблема наблюдается на всех счетчиках при установке значения 1000000. Требуется исправить отображение счетчиков для корректного отображения данных.

    Предложенное решение:
Исправить отображение счетчиков таким образом, чтобы при установке значения они отображались корректно.

5. #### BUG-5

    Название: Значение на счетчике не переводятся в более крупную систему измерения при значении 999999999 и неверное округление счетчиков при значении 999999999

    Приоритет: Medium

    Окружение: Linux/ Google Chrome Version 119.0.6045.159 (Official Build) (64-bit)

    Описание:
При значении 999999999 для счетчика на странице счетчиков на десктопной версии, счетчик отображает значение керроктное значение. Это приводит к некорректному отображению данных.

    Шаги воспроизведения:
   1. Зайти на страницу счетчиков на десктопной версии.
    2. Сделать подмену значений счетчиков на значение 999999999.
    3. Сделать скриншот каждого счетчика после установки значения.

    Ожидаемый результат:
Счетчики становятся черным
При переходе пограничных значений счетчики переводятся в более крупную систему измерения

    Фактический результат:
Счетчик отображает значение 1000.

    Скриншоты:
    ![](output/screen_test9_2024.04.14_16.58.15.png)

    Дополнительная информация:
Проблема наблюдается на всех счетчиках при установке значения. Требуется исправить отображение счетчиков для корректного отображения данных.

    Предложенное решение:
Исправить отображение счетчиков таким образом, чтобы при установке значения они отображали корректное значение.

6. #### BUG-6
    Название: Значение 1000001 на счетчике отображается некорректно

    Приоритет: High

    Окружение: Linux/ Google Chrome Version 119.0.6045.159 (Official Build) (64-bit)

    Описание:
При значении 1000001 для счетчика на странице счетчиков на десктопной версии текст на счетчике энергии перекрывает друг друга. Это приводит к некорректному отображению данных.

    Шаги воспроизведения:
   1. Зайти на страницу счетчиков на десктопной версии.
    2. Сделать подмену значений счетчиков на значение 1000001.
    3. Сделать скриншот каждого счетчика после установки значения.

    Ожидаемый результат:
Счетчики становятся черным
При переходе пограничных значений счетчики переводятся в более крупную систему измерения

    Фактический результат:
Текст на счетчике энергии перекрывает друг друга.

    Скриншоты:
    ![](output/screen_test6_2024.04.14_16.56.37.png)

    Дополнительная информация:
Проблема наблюдается на всех счетчиках при установке значения 1000001. Требуется исправить отображение счетчиков для корректного отображения данных.

    Предложенное решение:
Исправить отображение счетчиков таким образом, чтобы при установке значения они отображались корректно.

7. #### BUG-7

    Название: Значение на счетчике не переводятся в более крупную систему измерения при значении 9999999999999 и неверное округление счетчиков при значении 9999999999999

    Приоритет: Medium

    Окружение: Linux/ Google Chrome Version 119.0.6045.159 (Official Build) (64-bit)

    Описание:
При значении 9999999999999 для счетчика на странице счетчиков на десктопной версии, счетчик отображает значение керроктное значение. Это приводит к некорректному отображению данных.

    Шаги воспроизведения:
   1. Зайти на страницу счетчиков на десктопной версии.
    2. Сделать подмену значений счетчиков на значение 9999999999999.
    3. Сделать скриншот каждого счетчика после установки значения.

    Ожидаемый результат:
Счетчики становятся черным
При переходе пограничных значений счетчики переводятся в более крупную систему измерения

    Фактический результат:
Счетчик отображает значение 10.

    Скриншоты:
    ![](output/screen_test15_2024.04.14_17.00.49.png)

    Дополнительная информация:
Проблема наблюдается на всех счетчиках при установке значения. Требуется исправить отображение счетчиков для корректного отображения данных.

    Предложенное решение:
Исправить отображение счетчиков таким образом, чтобы при установке значения они отображали корректное значение.

8. #### BUG-8

    Название: Значение 1000000001 на счетчике отображается некорректно

    Приоритет: High

    Окружение: Linux/ Google Chrome Version 119.0.6045.159 (Official Build) (64-bit)

    Описание:
При значении 1000000001 для счетчика на странице счетчиков на десктопной версии текст на счетчике энергии перекрывает друг друга. Это приводит к некорректному отображению данных.

    Шаги воспроизведения:
   1. Зайти на страницу счетчиков на десктопной версии.
    2. Сделать подмену значений счетчиков на значение 1000000001.
    3. Сделать скриншот каждого счетчика после установки значения.

    Ожидаемый результат:
Счетчики становятся черным
При переходе пограничных значений счетчики переводятся в более крупную систему измерения

    Фактический результат:
Текст на счетчике энергии перекрывает друг друга.

    Скриншоты:
    ![](output/screen_test7_2024.04.14_16.57.22.png)

    Дополнительная информация:
Проблема наблюдается на всех счетчиках при установке значения 1000000001. Требуется исправить отображение счетчиков для корректного отображения данных.

    Предложенное решение:
Исправить отображение счетчиков таким образом, чтобы при установке значения они отображались корректно.

9. #### BUG-9

    Название: Значение 1000000000 на счетчике отображается некорректно

    Приоритет: High

    Окружение: Linux/ Google Chrome Version 119.0.6045.159 (Official Build) (64-bit)

    Описание:
При значении 1000000000 для счетчика на странице счетчиков на десктопной версии текст на счетчике энергии перекрывает друг друга. Это приводит к некорректному отображению данных.

    Шаги воспроизведения:
   1. Зайти на страницу счетчиков на десктопной версии.
    2. Сделать подмену значений счетчиков на значение 1000000000.
    3. Сделать скриншот каждого счетчика после установки значения.

    Ожидаемый результат:
Счетчики становятся черным
При переходе пограничных значений счетчики переводятся в более крупную систему измерения

    Фактический результат:
Текст на счетчике энергии перекрывает друг друга.

    Скриншоты:
    ![](output/screen_test8_2024.04.14_16.57.32.png)

    Дополнительная информация:
Проблема наблюдается на всех счетчиках при установке значения 1000000000. Требуется исправить отображение счетчиков для корректного отображения данных.

    Предложенное решение:
Исправить отображение счетчиков таким образом, чтобы при установке значения они отображались корректно.

10. #### BUG-10
    
    Название: Значение 999999999 на счетчике отображается некорректно

    Приоритет: High

    Окружение: Linux/ Google Chrome Version 119.0.6045.159 (Official Build) (64-bit)

    Описание:
При значении 999999999 для счетчика на странице счетчиков на десктопной версии текст на счетчике энергии перекрывает друг друга. Это приводит к некорректному отображению данных.

    Шаги воспроизведения:
    1. Зайти на страницу счетчиков на десктопной версии.
    2. Сделать подмену значений счетчиков на значение 999999999.
    3. Сделать скриншот каждого счетчика после установки значения.

    Ожидаемый результат:
Счетчики становятся черным
При переходе пограничных значений счетчики переводятся в более крупную систему измерения

    Фактический результат:
Текст на счетчике энергии перекрывает друг друга.

    Скриншоты:
    ![](output/screen_test9_2024.04.14_16.58.15.png)

    Дополнительная информация:
Проблема наблюдается на всех счетчиках при установке значения 1000000000. Требуется исправить отображение счетчиков для корректного отображения данных.

    Предложенное решение:
Исправить отображение счетчиков таким образом, чтобы при установке значения они отображались корректно.

11. #### BUG-11

    Название: Значение 1000000000000 на счетчике отображается некорректно

    Приоритет: High

    Окружение: Linux/ Google Chrome Version 119.0.6045.159 (Official Build) (64-bit)

    Описание:
При значении 1000000000000 для счетчика на странице счетчиков на десктопной версии текст на счетчике энергии перекрывает друг друга. Это приводит к некорректному отображению данных.

    Шаги воспроизведения:
    1. Зайти на страницу счетчиков на десктопной версии.
    2. Сделать подмену значений счетчиков на значение 1000000000000.
    3. Сделать скриншот каждого счетчика после установки значения.

    Ожидаемый результат:
Счетчики становятся черным
При переходе пограничных значений счетчики переводятся в более крупную систему измерения

    Фактический результат:
Текст на счетчике энергии перекрывает друг друга.

    Скриншоты:
    ![](output/screen_test14_2024.04.14_17.00.10.png)

    Дополнительная информация:
Проблема наблюдается на всех счетчиках при установке значения 1000000000000. Требуется исправить отображение счетчиков для корректного отображения данных.

    Предложенное решение:
Исправить отображение счетчиков таким образом, чтобы при установке значения они отображались корректно.

12. #### BUG-12

     Название: Значение 9999999999999 на счетчике отображается некорректно

    Приоритет: High

    Окружение: Linux/ Google Chrome Version 119.0.6045.159 (Official Build) (64-bit)

    Описание:
При значении 9999999999999 для счетчика на странице счетчиков на десктопной версии текст на счетчике энергии перекрывает друг друга. Это приводит к некорректному отображению данных.

    Шаги воспроизведения:
    1. Зайти на страницу счетчиков на десктопной версии.
    2. Сделать подмену значений счетчиков на значение 9999999999999.
    3. Сделать скриншот каждого счетчика после установки значения.

    Ожидаемый результат:
Счетчики становятся черным
При переходе пограничных значений счетчики переводятся в более крупную систему измерения

    Фактический результат:
Текст на счетчике энергии перекрывает друг друга.

    Скриншоты:
    ![](output/screen_test15_2024.04.14_17.00.49.png)

    Дополнительная информация:
Проблема наблюдается на всех счетчиках при установке значения 9999999999999. Требуется исправить отображение счетчиков для корректного отображения данных.

    Предложенное решение:
Исправить отображение счетчиков таким образом, чтобы при установке значения они отображались корректно.